[build]
builder = "RAILPACK"
buildCommand = "npm ci && npm run build"
watchPatterns = ["**"]

[deploy]
# Bind all interfaces so Railway's proxy can reach the process
startCommand = "HOST=0.0.0.0 node build/index.js"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
